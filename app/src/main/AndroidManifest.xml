<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ourduty.digitalsignv03" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <activity android:name=".OneActivity">android:label="OneActivity"</activity>
                <activity android:name=".TwoActivity">android:label="TwoActivity"</activity>
                <activity android:name=".TaskModeInput">android:label="TaskMode"</activity>
                <activity android:name=".SettingsActivity">android:label="Settings"</activity>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
